# AI Configuration — 激活快照 (v0.5.7)
# ⚠️ 此文件由面板从 ai_profiles.yaml 的 active profile 自动生成,
#    仅作 guard 热加载快照 — 不要手动编辑!
#
# 编辑 AI 配置请使用面板设置页或 ai_profiles.yaml:
#   config/ai_profiles.yaml.example  →  ai_profiles.yaml (gitignored)
#
# 结构 (与 ai_profiles.yaml 的 profile 一致):
#   api_key / model / base_url / auto_response_threshold / pending_review_threshold

api_key: "sk-your-api-key-here"
model: "deepseek-chat"
base_url: "https://api.deepseek.com/v1"
auto_response_threshold: 85
pending_review_threshold: 60
